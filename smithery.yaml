startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
    additionalProperties: false
    description: "No configuration required. The plsreadme MCP server works out of the box with zero setup."
  commandFunction:
    - npx
    - -y
    - plsreadme-mcp
